# Build dependencies for PyInstaller packaging
# Project runtime dependencies
PySide6>=6.5.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
ultralytics>=8.0.0
# torch directly imported by train_dialog / export_dialog / yolo_train;
# previously provided only as a transitive dependency of ultralytics.
# CUDA wheels: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
pyyaml>=6.0
dxcam>=0.3.0; sys_platform == "win32"  # Windows-only screen capture

# Model export dependencies (bundled into package)
onnx>=1.12.0
onnxslim>=0.1.71
onnxruntime-gpu  # GPU version; use onnxruntime for CPU
openvino>=2024.0.0
# tensorflow  # TFLite/SavedModel export (~1GB, uncomment if needed)
# paddlepaddle  # PaddlePaddle export (~500MB, uncomment if needed)

# Packaging tools
pyinstaller>=6.0.0
# UPX: https://upx.github.io/
