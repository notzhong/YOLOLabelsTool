# 打包构建依赖
# 注意：除了项目运行时依赖外，还需要以下打包工具

# 项目运行时依赖
PySide6>=6.5.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
ultralytics>=8.0.0
pyyaml>=6.0

# 打包工具
pyinstaller>=6.0.0
# UPX 压缩工具（可选，但推荐）
# upx 需要单独下载：https://upx.github.io/

# 可选：用于创建安装程序的工具
# inno-setup  # Windows安装程序，需要单独下载

# 打包时可能需要额外注意的依赖：
# torch 和 torchvision 通常通过 ultralytics 依赖，但可能需要特定版本
# 如果遇到CUDA相关错误，可以尝试：
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118