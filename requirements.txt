PySide6>=6.5.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
ultralytics>=8.0.0
pyyaml>=6.0
dxcam>=0.3.0

# 模型导出依赖（按需安装，不打包含注释即可）
onnx>=1.12.0
onnxslim>=0.1.71
onnxruntime-gpu  # GPU版本；CPU环境改用 onnxruntime
openvino>=2024.0.0
# tensorflow  # TFLite/SavedModel 导出（体量 ~1GB，按需取消注释）
# paddlepaddle  # PaddlePaddle 导出（体量 ~500MB，按需取消注释）
