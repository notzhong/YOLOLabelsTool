PySide6>=6.5.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
ultralytics>=8.0.0
# torch 由代码直接 import（train_dialog / export_dialog / yolo_train），
# 此前仅靠 ultralytics 传递依赖提供；显式声明避免上游依赖变化导致运行时崩溃
torch>=2.0.0
pyyaml>=6.0
dxcam>=0.3.0; sys_platform == "win32"  # 仅 Windows 屏幕采集，非 Windows 平台不安装

# 模型导出依赖已拆分为可选（约 540MB，不导出模型无需安装）：
#   pip install -r requirements-export.txt        # CPU 版（默认）
#   或 pip install -e ".[export]"                 # 等价 extras
# 也可以不预装：在导出对话框首次导出时按提示一键安装。
# 注意：打包（build.bat）仍要求全量安装导出依赖，见 requirements-build.txt。
